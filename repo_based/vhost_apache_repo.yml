---
- name: "Apache vhost 표준 구성 작전 (repo 기반)"
  hosts: apache_servers
  become: yes
  vars:
    domain_name: "my-service.local"
    doc_root: "/var/www/{{ domain_name }}"
    listen_port: 80  # 현재 사용 중인 포트

  tasks:
    - name: "STEP 1: 웹 루트 디렉토리 생성"
      file:
        path: "{{ doc_root }}"
        state: directory
        owner: www-data
        group: www-data
        mode: '0755'

    - name: "STEP 2: vhost 설정 파일 생성"
      copy:
        dest: "/etc/apache2/sites-available/{{ domain_name }}.conf"
        owner: root
        group: root
        mode: '0644'
        content: |
          <VirtualHost *:{{ listen_port }}>
              ServerName {{ domain_name }}
              DocumentRoot {{ doc_root }}

              <Directory {{ doc_root }}>
                  Options Indexes FollowSymLinks
                  AllowOverride All
                  Require all granted
              </Directory>

              ErrorLog ${APACHE_LOG_DIR}/{{ domain_name }}_error.log
              CustomLog ${APACHE_LOG_DIR}/{{ domain_name }}_access.log combined
          </VirtualHost>

    - name: "STEP 3: 사이트 활성화 (a2ensite)"
      command: "a2ensite {{ domain_name }}.conf"
      args:
        creates: "/etc/apache2/sites-enabled/{{ domain_name }}.conf"

    - name: "STEP 4: 기본 사이트 비활성화"
      command: "a2dissite 000-default.conf"
      ignore_errors: yes

    - name: "STEP 5: 테스트용 index.html 생성"
      copy:
        dest: "{{ doc_root }}/index.html"
        content: "<h1>{{ domain_name }} vhost 작전 성공! 사령관님 가즈아!</h1>"
        owner: www-data
        group: www-data
        mode: '0644'

    - name: "STEP 6: 아파치 설정 재로드"
      service:
        name: apache2
        state: reloaded

    - name: "📢 [최종 작전 보고서]"
      debug:
        msg: 
          - "=========================================="
          - "   APACHE VHOST 배포가 완료되었습니다!"
          - "=========================================="
          - "📍 접속 도메인: {{ domain_name }}"
          - "📍 접속 포트  : {{ listen_port }}"
          - "📍 웹 루트 경로: {{ doc_root }}"
          - "📍 서버 아이피: {{ ansible_host | default(inventory_hostname) }}"
          - "=========================================="
